import * as React from "react";
import {Typography} from "@material-ui/core";

export class PossessionsPage extends React.Component {
    public render(): JSX.Element {
        return (<Typography noWrap={false}>{"Possessions Page"}</Typography>);
    }
}
